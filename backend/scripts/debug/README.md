# Debug Helpers

These scripts are ad-hoc local helpers for diagnosing the demo environment.

## Notes

- Run them from the repo root unless the script says otherwise.
- Scripts that call authenticated API routes may need `SESSION_COOKIE` set from a logged-in browser session.
